import { Entity, PrimaryKey, Property } from '@mikro-orm/core';
import { Field, Int, ObjectType } from 'type-graphql';

@ObjectType()
@Entity()
export default class Doctor {

  @Field(() => Int)
  @PrimaryKey()
  id: number;

  @Field(() => String)
  @Property()
  firstName: string;

  @Field(() => String)
  @Property()
  lastName: string;

  @Field(() => String)
  @Property()
  email: string;
  
  @Field(() => String)
  @Property()
  password: string;

  @Field(() => String) 
  @Property({default: 'en_GB'}) // add default language as english. Need to ask FE team and decide on convention
  language: string;
  
  @Field(() => String)
  @Property() // have autogenerated code tbc.
  docPublicCode: string;
}


// doctor: {
//   _id: ID;
//   firstname: string;
//   lastname: string;
//   email: string;
//   password: string;
//   language: string; // two letter code, EN by default
//   docPublicCode: string; // public code to be entered by the patient
// }